# Fedora only — or names that differ from Arch.
fd-find                   # Arch: fd
pipx                      # Arch: python-pipx
jetbrains-mono-fonts      # Arch: ttf-jetbrains-mono
man-db

# --- containerised toolchains (ADR-0012) ------------------------------
# ThinkPad only: coursework tooling goes in a distrobox, not on the host.
# The desktop deliberately does NOT get this — AUR is already its escape
# hatch, and principle #7 says the desktop is allowed to be messy.
distrobox
podman
python3-pyyaml            # required by dotbot; Arch: python-yaml

# --- prompt (ADR-0010: shared by fish and bash) -----------------------
# NOT in common.txt even though the name matches Arch: Fedora dropped
# starship from its repos (it was there until ~F37) and it now comes from
# a COPR, which bootstrap.sh does not enable. Same shape as the `lame` /
# RPM Fusion note in cachyos.txt.
#
#   sudo dnf copr enable -y atim/starship
#
# Until that is run by hand, this line is a no-op with --skip-unavailable.
starship

# --- LaTeX toolchain (ADR-0018) ---------------------------------------
# THE COMPILER ITSELF WAS DECLARED NOWHERE. Not here, not common.txt, not
# cachyos.txt, not a bundle. common.txt has a whole section arguing zathura is
# wanted on "every machine that edits LaTeX, which is all of them" — and the
# thing that produces the PDF zathura opens was in no list at all. A rebuilt
# machine got the editor, vimtex and the viewer, and no way to compile.
#
# Same shape as btop, wl-clipboard, libnotify and polkit-kde on 2026-08-24:
# true-but-undeclared. latexmk was genuinely absent from the host while it
# lived in the `uni` distrobox; ADR-0018 moved TeX Live onto the host and the
# package lists never caught up. Found 2026-08-26.
#
# Names read off this machine with `rpm -qf` / `rpm -q`, not guessed:
# latexmk-4.88, texlive-minted-svn78270, texlive-collection-latex-svn78733.
# Fedora splits these three ways and Arch does not, so all three differ.
latexmk                   # Arch: texlive-binextra
texlive-collection-latex  # Arch: texlive-latex
texlive-minted            # Arch: texlive-latexextra (a much coarser split —
                          # ~110 MB there vs a few MB here; worth pricing
                          # before adding the Arch side to cachyos.txt)

# --- LaTeX code highlighting (ADR-0018) -------------------------------
# pygmentize, which the minted package shells out to. Not in common.txt
# because Arch spells it python-pygments.
#
# This came free with the `uni` distrobox image for as long as latexmk lived
# there. ADR-0018 moved TeX Live onto the host; pygments did not follow, and
# nothing noticed, because latexmk reports the failure as a shell-escape error
# rather than naming the binary it could not find.
#
# Verified 2026-08-26: `command -v pygmentize` found nothing, while
# `dnf provides '*/bin/pygmentize'` names this package. Seven documents use
# minted, three of them the shared UTAD preambles (2020, 2021, 2025), so
# everything inheriting those years had stopped compiling.
#
# config/nvim/lua/plugins/latex.lua passes -shell-escape for exactly this.
python3-pygments          # Arch: python-pygments
